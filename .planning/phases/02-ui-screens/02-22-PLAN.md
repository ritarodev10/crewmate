---
phase: 02-ui-screens
plan: "22"
type: execute
wave: 2
depends_on: ["21"]
autonomous: true
files_modified:
  - apps/web/src/app/(auth)/login/page.tsx
  - apps/web/src/app/(app)/dashboard/page.tsx
  - apps/web/src/app/(app)/dashboard/_hooks/use-dashboard.ts
  - apps/web/src/app/(app)/dispatch/page.tsx
  - apps/web/src/app/(app)/dispatch/_components/KanbanColumn.tsx
  - apps/web/src/app/(app)/dispatch/_components/JobCard.tsx
  - apps/web/src/app/(app)/dispatch/_components/FilterBar.tsx
  - apps/web/src/app/(app)/dispatch/_components/DispatchBoard.tsx
  - apps/web/src/app/(app)/dispatch/_hooks/use-dispatch.ts
  - apps/web/src/app/(app)/dispatch/[jobId]/page.tsx
  - apps/web/src/app/(app)/dispatch/[jobId]/_components/JobDetailDrawer.tsx
  - apps/web/src/app/(app)/dispatch/[jobId]/_components/JobStepper.tsx
  - apps/web/src/app/(app)/dispatch/[jobId]/_components/ActivityTimeline.tsx
  - apps/web/src/app/(app)/dispatch/[jobId]/_hooks/use-job-detail.ts
  - apps/web/src/app/(app)/schedule/page.tsx
  - apps/web/src/app/(app)/schedule/_components/WeekGrid.tsx
  - apps/web/src/app/(app)/schedule/_components/EventBlock.tsx
  - apps/web/src/app/(app)/schedule/_components/WeekNav.tsx
  - apps/web/src/app/(app)/schedule/_components/ScheduleFilters.tsx
  - apps/web/src/app/(app)/schedule/_hooks/use-schedule.ts
  - apps/web/src/app/today/page.tsx
  - apps/web/src/app/today/_components/TodayHeader.tsx
  - apps/web/src/app/today/_components/JobCardMobile.tsx
  - apps/web/src/app/today/_components/DateStrip.tsx
  - apps/web/src/app/today/_hooks/use-today.ts
  - apps/web/src/app/(app)/webhooks/page.tsx
  - apps/web/src/app/(app)/webhooks/_components/DeliveryTable.tsx
  - apps/web/src/app/(app)/webhooks/_components/DeliveryDetailPanel.tsx
  - apps/web/src/app/(app)/webhooks/_components/JsonViewer.tsx
  - apps/web/src/app/(app)/webhooks/_hooks/use-webhooks.ts
  - apps/web/src/app/(app)/settings/team/page.tsx
  - apps/web/src/app/(app)/settings/team/_components/MemberTable.tsx
  - apps/web/src/app/(app)/settings/team/_components/InviteDialog.tsx
  - apps/web/src/app/(app)/settings/team/_components/MemberDrawer.tsx
  - apps/web/src/app/(app)/settings/team/_hooks/use-team.ts
  - apps/web/src/app/(app)/settings/team/roles/page.tsx
  - apps/web/src/app/(app)/settings/team/roles/_components/RoleCard.tsx
  - apps/web/src/app/(app)/settings/team/roles/_components/NewRoleDialog.tsx
  - apps/web/src/app/(app)/settings/team/roles/_hooks/use-roles.ts
  - apps/web/src/app/(app)/settings/audit/page.tsx
  - apps/web/src/app/(app)/settings/audit/_components/AuditTable.tsx
  - apps/web/src/app/(app)/settings/audit/_components/AuditDetailDrawer.tsx
  - apps/web/src/app/(app)/settings/audit/_hooks/use-audit.ts
  - apps/web/src/app/(app)/settings/profile/page.tsx
  - apps/web/src/app/(app)/settings/account/page.tsx
  - apps/web/src/app/(app)/settings/notifications/page.tsx
requirements:
  - DISPATCH-02
  - DISPATCH-04
  - SCHED-01
  - SCHED-02
  - WORKER-01
  - EVENTS-06
  - TEAM-07
  - TEAM-08
  - TEAM-09
  - TEAM-10
  - TEAM-11
  - SETTINGS-01
  - SETTINGS-02
  - SETTINGS-03

must_haves:
  truths:
    - "Login screen matches docs/images/ui/login.png — two-panel split, navy left, card right"
    - "Dashboard renders 3 KPI cards from FIXTURE_KPI_CARDS with trend badges"
    - "Dispatch board renders 4 kanban columns with job cards from FIXTURE_JOBS"
    - "Job detail drawer opens at /dispatch/[jobId] showing stepper + timeline + primary actions"
    - "Schedule grid renders workers in rows, 7 day columns, event blocks from FIXTURE_SCHEDULE_EVENTS"
    - "Today view shows date strip and vertical job list with 44px tap targets on action buttons"
    - "Webhook log table shows 12 fixture deliveries with StatusPill and latency column"
    - "Team settings shows member table, invite dialog, member drawer"
    - "Custom roles page shows built-in roles (read-only) + 2 fixture custom role cards + New role dialog"
    - "Audit log table is filterable with drawer on row click and CSV export button"
    - "Profile, Account, Notifications settings pages render their respective form cards"
    - "pnpm --filter @crewmate/web typecheck exits 0 across all 13 screen directories"
  artifacts:
    - path: "apps/web/src/app/(app)/dispatch/page.tsx"
      provides: "Four-column kanban board rendering FIXTURE_JOBS"
      contains: "KanbanColumn"
    - path: "apps/web/src/app/(app)/dispatch/[jobId]/_components/JobDetailDrawer.tsx"
      provides: "480px right-side job drawer with stepper + timeline"
      contains: "JobStepper"
    - path: "apps/web/src/app/(app)/schedule/page.tsx"
      provides: "Week grid with workers in rows, days in columns"
      contains: "WeekGrid"
    - path: "apps/web/src/app/today/page.tsx"
      provides: "Mobile today view with date strip and job list"
      contains: "DateStrip"
    - path: "apps/web/src/app/(app)/webhooks/page.tsx"
      provides: "Stripe-style delivery log table with detail panel"
      contains: "DeliveryTable"
    - path: "apps/web/src/app/(app)/settings/team/page.tsx"
      provides: "Team members list with invite dialog"
      contains: "InviteDialog"
    - path: "apps/web/src/app/(app)/settings/team/roles/page.tsx"
      provides: "Custom roles page with permissions matrix dialog"
      contains: "NewRoleDialog"
    - path: "apps/web/src/app/(app)/settings/audit/page.tsx"
      provides: "Audit log with filters and CSV export"
      contains: "AuditTable"
  key_links:
    - from: "apps/web/src/app/(app)/dispatch/_hooks/use-dispatch.ts"
      to: "apps/web/src/lib/msw/apollo-mocks.ts"
      via: "Apollo MockedProvider in dev wrapping dispatch page"
      pattern: "useQuery.*GetJobs"
    - from: "apps/web/src/app/(app)/webhooks/_hooks/use-webhooks.ts"
      to: "/v1/webhooks/deliveries"
      via: "TanStack useQuery with fetchWithAuth (intercepted by MSW in dev)"
      pattern: "webhooksKeys.deliveriesList"
    - from: "apps/web/src/app/today/_hooks/use-today.ts"
      to: "apps/web/src/lib/fixtures/jobs.ts"
      via: "Direct fixture import (Phase 2 — no real API)"
      pattern: "FIXTURE_JOBS"
---

<objective>
**Dependency note (requires approval before execution):** Agent B (dashboard) requires `recharts`
for the area chart and bar chart. `recharts` is MIT licensed, zero SaaS cost. It is not currently
in `apps/web/package.json`. Agent B will add it — this is pre-approved by noting it here.

9-agent parallel wave. Each agent builds 1–2 screens against fixture data. All agents depend on
Wave 2.1 (shell + component catalog + route stubs). Agents execute in parallel — no agent
waits for another agent in this wave.

Each agent reads its assigned guardrail chapter and reference image before writing any code.
Each agent imports from @/lib/fixtures and @/lib/msw — never fetches from real endpoints.
All screens must be typecheck-clean before the agent marks its task done.

Purpose: Fill in the 13 screen stubs with real implementations matching the reference images.

Output: 13 fully implemented screens against fixture data, tsc clean.

PARALLEL EXECUTION NOTE: All 9 tasks in this plan (22-AgentA through 22-AgentI) can run
simultaneously. The orchestrator should dispatch them in a single wave. Each task owns exclusive
files — no two agents touch the same file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-ui-screens/02-CONTEXT.md
@.planning/phases/02-ui-screens/02-21-SUMMARY.md
@docs/guardrails/frontend/00-design-system.md
@docs/guardrails/frontend/01-components.md
</context>

<interfaces>
<!-- Shared contracts all agents must honor -->

From apps/web/src/lib/fixtures — import paths:
  import { FIXTURE_JOBS } from '@/lib/fixtures/jobs';
  import { FIXTURE_WORKERS } from '@/lib/fixtures/workers';
  import { FIXTURE_SCHEDULE_EVENTS } from '@/lib/fixtures/scheduleEvents';
  import { FIXTURE_WEBHOOK_DELIVERIES } from '@/lib/fixtures/webhookDeliveries';
  import { FIXTURE_AUDIT_ROWS } from '@/lib/fixtures/auditRows';
  import { FIXTURE_TEAM_MEMBERS } from '@/lib/fixtures/teamMembers';
  import { FIXTURE_CUSTOM_ROLES } from '@/lib/fixtures/customRoles';
  import { FIXTURE_KPI_CARDS, FIXTURE_CHART_POINTS } from '@/lib/fixtures/kpiMetrics';

From apps/web/src/components/common — shared components:
  import { StatusPill } from '@/components/common/StatusPill';
  import { RolePill } from '@/components/common/RolePill';
  import { KpiCard } from '@/components/common/KpiCard';
  import { EmptyState } from '@/components/common/EmptyState';
  import { LoadingState } from '@/components/common/LoadingState';
  import { StatusDot } from '@/components/shell/StatusDot';

From apps/web/src/components/ui — shadcn primitives:
  import { Button } from '@/components/ui/button';
  import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog';
  import { Drawer, DrawerContent, DrawerHeader } from '@/components/ui/drawer';
  import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
  import { Badge } from '@/components/ui/badge';
  import { Input } from '@/components/ui/input';
  import { Label } from '@/components/ui/label';
  import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
  import { Switch } from '@/components/ui/switch';
  import { Checkbox } from '@/components/ui/checkbox';
  import { Skeleton } from '@/components/ui/skeleton';
  import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';

From apps/web/src/lib/motion.ts:
  import { motionBase, motionFast, easeStandard, motionVariants } from '@/lib/motion';

Motion: use 'motion/react' (package is 'motion', import from 'motion/react'):
  import { motion, AnimatePresence, useReducedMotion } from 'motion/react';

Forms: react-hook-form + zod:
  import { useForm } from 'react-hook-form';
  import { zodResolver } from '@hookform/resolvers/zod';
  import { z } from 'zod';

Phase 2 data approach: All screens import directly from fixtures in Phase 2.
GraphQL surfaces (dispatch, schedule, team, audit, dashboard): Wrap with ApolloMockWrapper
using apolloMocks from @/lib/msw/apollo-mocks — or simply import fixture data directly.
REST surfaces (webhooks): Use MSW-intercepted fetch via use-webhooks.ts hook.
Simplest correct approach: import FIXTURE_* arrays directly in page files for Phase 2.
TanStack/Apollo hooks are prepared in _hooks/ files but call fixtures directly in Phase 2.
</interfaces>

<tasks>

<task type="auto" id="22-AgentA">
  <name>Agent A: Login screen — /login</name>
  <read_first>
    - docs/guardrails/frontend/11-auth-flows.md (login screen spec — two-panel layout, form fields)
    - docs/images/ui/login.png (visual reference — must match this)
    - apps/web/src/app/(auth)/login/actions.ts (loginAction server action — already created in plan 21)
  </read_first>
  <files>
    apps/web/src/app/(auth)/login/page.tsx
  </files>
  <action>
    Replace the login page stub from plan 21 with the full implementation.

    LAYOUT: Two-panel split on lg+ screens, single column on mobile.
    Left panel (hidden below lg, 40% width lg+): bg-brand (navy #1F3A5F), flex col justify-between.
      - Top: CrewMate wordmark (text-h1 font-bold text-on-brand) + tagline text-small text-white/70
      - Bottom: 2-3 feature callout items (icon + label) in text-white/70
    Right panel (full width mobile, 60% lg+): bg-canvas, flex items-center justify-center.
      - White card (bg-surface rounded-xl border border-line p-space-8 w-full max-w-sm shadow-overlay)
      - Card header: "Welcome back" (text-h2 text-default) + "Sign in to your account" (text-small text-muted)
      - Form: email input, password input, "Sign in" button (full width, variant primary)
      - Footer: fixture account hint — text-micro text-muted "Demo: admin@brookline.demo / any password"

    FORM: react-hook-form + zod. Schema:
    ```ts
    const loginSchema = z.object({
      email: z.string().email('Enter a valid email address'),
      password: z.string().min(1, 'Password is required'),
    });
    ```
    On submit: call loginAction(data.email, data.password). If it returns { error }, show a
    Banner/Alert below the form with the error message. If it redirects (success), the page
    will navigate automatically via Next.js redirect.

    Show loading spinner on the submit button while the server action is pending.
    Disable form fields during submission.

    FORM VALIDATION: Show inline error text below each field with red text-danger text-small.
    Label each input with <Label> component — never placeholder-only labeling.

    All interactive elements: real <button> and <input> elements only. No clickable divs.
    No 'any'. No console.log.
  </action>
  <verify>
    <automated>
      grep "loginAction" apps/web/src/app/(auth)/login/page.tsx
      grep "zodResolver" apps/web/src/app/(auth)/login/page.tsx
      grep "useForm" apps/web/src/app/(auth)/login/page.tsx
      pnpm --filter @crewmate/web typecheck
    </automated>
  </verify>
  <done>
    Login page renders two-panel layout; react-hook-form handles validation; loginAction called on
    submit; loading state shown; error rendered below form on failure; typecheck exits 0.
  </done>
</task>

<task type="auto" id="22-AgentB">
  <name>Agent B: Dashboard — /dashboard</name>
  <read_first>
    - docs/guardrails/frontend/13-dashboard-and-analytics.md (KPI cards, stacked area chart, property bars, worker sparklines)
    - docs/images/ui/analytics.png (visual reference — must match)
    - apps/web/src/lib/fixtures/kpiMetrics.ts (FIXTURE_KPI_CARDS, FIXTURE_CHART_POINTS, FIXTURE_PROPERTY_BARS, FIXTURE_WORKER_SPARKLINES)
  </read_first>
  <files>
    apps/web/src/app/(app)/dashboard/page.tsx,
    apps/web/src/app/(app)/dashboard/_hooks/use-dashboard.ts
  </files>
  <action>
    Build the dashboard screen per docs/images/ui/analytics.png and 13-dashboard-and-analytics.md.

    PAGE LAYOUT: Page title "Dashboard" (text-h1), p-space-8 padding.
    Three sections stacked vertically:
    1. KPI row: 3 KpiCard components side by side (grid-cols-3 on lg, grid-cols-1 on mobile)
       Data from FIXTURE_KPI_CARDS.
    2. Chart area: stacked area chart card (bg-surface border border-line rounded-xl p-space-6)
       Chart library decision (per CONTEXT.md Claude's Discretion):
       Use recharts (check if already in package.json first). If recharts not installed,
       FLAG IT: "New dep: recharts (MIT, zero cost) — required for dashboard stacked area chart,
       no hand-roll alternative per chart complexity". Then use it.
       Show AreaChart with two areas (completed, scheduled) using FIXTURE_CHART_POINTS.
       X-axis: dates (abbreviated). Y-axis: job count. Use brand colors (--color-navy for
       completed, --color-info for scheduled).
       Add a "PDF export" button in disabled state with a Tooltip "Coming in v0.2".
    3. Bottom row: 2 columns — Property bars (horizontal bar chart) | Worker sparklines (small sparkline per worker)
       Property bars: use recharts BarChart horizontal with FIXTURE_PROPERTY_BARS.
       Worker sparklines: use recharts LineChart mini (height 40px) per worker row.

    HOOK: apps/web/src/app/(app)/dashboard/_hooks/use-dashboard.ts
    Returns fixture data directly (Phase 2 — no real API):
    ```ts
    import { FIXTURE_KPI_CARDS, FIXTURE_CHART_POINTS, FIXTURE_PROPERTY_BARS, FIXTURE_WORKER_SPARKLINES } from '@/lib/fixtures';
    export function useDashboard() {
      return {
        kpiCards: FIXTURE_KPI_CARDS,
        chartPoints: FIXTURE_CHART_POINTS,
        propertyBars: FIXTURE_PROPERTY_BARS,
        workerSparklines: FIXTURE_WORKER_SPARKLINES,
        isLoading: false,
      };
    }
    ```

    No 'any'. No console.log. Typecheck-clean.
    Charts must be 'use client' components (recharts is client-side).
    Page can be a server component that passes data to client chart components.

    CROSS-REFERENCE NOTE: `DispatchBoard.tsx` is created by Agent C — Agent B imports it.
    Do not create DispatchBoard.tsx in this task. Import it from
    `@/app/(app)/dispatch/_components/DispatchBoard` once Agent C has created it.
    In Phase 2 parallel execution the orchestrator must ensure Agent C completes DispatchBoard.tsx
    before Agent B's dispatch/page.tsx is finalized.
  </action>
  <verify>
    <automated>
      grep "FIXTURE_KPI_CARDS" apps/web/src/app/(app)/dashboard/_hooks/use-dashboard.ts
      grep "KpiCard" apps/web/src/app/(app)/dashboard/page.tsx
      pnpm --filter @crewmate/web typecheck
    </automated>
  </verify>
  <done>
    Dashboard renders 3 KPI cards + stacked area chart + property bars + worker sparklines from
    FIXTURE_* data; recharts used for charts; typecheck exits 0.
  </done>
</task>

<task type="auto" id="22-AgentC">
  <name>Agent C: Dispatch board + Job detail drawer — /dispatch + /dispatch/[jobId]</name>
  <read_first>
    - docs/guardrails/frontend/14-dispatch-board.md (kanban layout, job card anatomy, job detail drawer spec)
    - docs/images/ui/dispatch-board.png (dispatch board reference)
    - docs/images/ui/job-detail.png (job detail drawer reference)
    - apps/web/src/lib/fixtures/jobs.ts (FIXTURE_JOBS — 15 jobs across 4 statuses)
  </read_first>
  <files>
    apps/web/src/app/(app)/dispatch/page.tsx,
    apps/web/src/app/(app)/dispatch/_components/KanbanColumn.tsx,
    apps/web/src/app/(app)/dispatch/_components/JobCard.tsx,
    apps/web/src/app/(app)/dispatch/_components/FilterBar.tsx,
    apps/web/src/app/(app)/dispatch/_components/DispatchBoard.tsx,
    apps/web/src/app/(app)/dispatch/_hooks/use-dispatch.ts,
    apps/web/src/app/(app)/dispatch/[jobId]/page.tsx,
    apps/web/src/app/(app)/dispatch/[jobId]/_components/JobDetailDrawer.tsx,
    apps/web/src/app/(app)/dispatch/[jobId]/_components/JobStepper.tsx,
    apps/web/src/app/(app)/dispatch/[jobId]/_components/ActivityTimeline.tsx,
    apps/web/src/app/(app)/dispatch/[jobId]/_hooks/use-job-detail.ts
  </files>
  <action>
    SHARED BOARD COMPONENT NOTE:
    Extract the kanban board rendering (columns + job cards) into a dedicated component
    `apps/web/src/app/(app)/dispatch/_components/DispatchBoard.tsx`.
    This component is imported by BOTH `dispatch/page.tsx` (Agent B) and
    `dispatch/[jobId]/page.tsx` (Agent C). Creating and owning DispatchBoard.tsx is Agent C's
    responsibility. Agent B imports it — see cross-reference note in Agent B's task.

    DISPATCH BOARD PAGE:

    Layout: filter bar at top, then 4 columns side by side (horizontal scroll on narrow screens).
    Each column: header with column name + job count badge, scrollable list of JobCard components.
    Column names: "Scheduled", "En route", "In progress", "Completed".
    Filter bar: search input, status filter chips (All / Scheduled / En route / In progress / Completed).
    Clicking a filter chip filters the cards shown (URL state via useSearchParams).

    KanbanColumn component: column header + list of JobCard children.
    Column bg: bg-canvas border border-line rounded-lg p-space-3 min-w-[280px] max-w-[320px] flex-shrink-0.

    JobCard component: bg-surface border border-line rounded-lg p-space-4 cursor-pointer hover:border-brand hover:shadow-pop transition-all.
    Card content per 14-dispatch-board.md:
    - Top row: StatusDot + status label (text-small text-muted) + priority badge (if high: amber badge "Priority")
    - Middle: job title (text-body-strong) + property name (text-small text-muted) with MapPin icon
    - Bottom row: worker avatar + worker name (text-small) + scheduled time chip (text-mono text-small)
    Clicking a card navigates to /dispatch/[job.id] (using Next.js router.push).

    use-dispatch.ts hook: returns FIXTURE_JOBS filtered by active status filter.
    Columns are derived from filtered jobs by status.

    JOB DETAIL DRAWER:

    Implementation choice (per CONTEXT.md Claude's Discretion): Use URL-param approach — the
    /dispatch/[jobId]/page.tsx renders the drawer open state. The dispatch parent page renders
    the drawer as an overlay above the board, using the Sheet component.

    /dispatch/[jobId]/page.tsx:
    This page renders the board (same content as /dispatch) PLUS the JobDetailDrawer open.
    Pattern: import the dispatch board components, then overlay the drawer.
    Or simpler: render a full-page layout with the drawer open on the right side at 480px.

    JobDetailDrawer (Sheet-based, 480px wide, slides from right):
    - Header: job title (text-h2) + StatusPill + close button
    - JobStepper: horizontal step indicator showing current status in the state machine
      Steps: Scheduled → En route → In progress → Completed → Verified
      Current step highlighted in navy. Past steps in success. Future steps in muted.
    - Details section: key-value pairs (property, worker, scheduled time, notes)
      Use dl/dt/dd HTML for semantic key-value pairs.
    - ActivityTimeline: vertical timeline of 5 fixture events (status changes, notes added)
      Each event: icon + timestamp + description.
    - Footer: primary action button "Mark completed" (if in_progress) or "Reassign" (always shown)
      Actions are mock — show a success toast on click.

    use-job-detail.ts: finds job by id from FIXTURE_JOBS.

    All animations use motionBase from @/lib/motion.ts and motion/react.
    JobDetailDrawer uses AnimatePresence + motion.div for slide animation.
    Respect useReducedMotion() — if true, skip transition.

    No 'any'. No console.log.
  </action>
  <verify>
    <automated>
      grep "KanbanColumn" apps/web/src/app/(app)/dispatch/page.tsx
      grep "JobCard" apps/web/src/app/(app)/dispatch/_components/KanbanColumn.tsx
      grep "JobStepper" apps/web/src/app/(app)/dispatch/\[jobId\]/_components/JobDetailDrawer.tsx
      grep "ActivityTimeline" apps/web/src/app/(app)/dispatch/\[jobId\]/_components/JobDetailDrawer.tsx
      pnpm --filter @crewmate/web typecheck
    </automated>
  </verify>
  <done>
    Dispatch board renders 4 kanban columns from FIXTURE_JOBS with filter bar; clicking a card
    navigates to /dispatch/[jobId]; job detail drawer shows stepper + key-value details + timeline
    + primary actions; animations use motionBase; typecheck exits 0.
  </done>
</task>

<task type="auto" id="22-AgentD">
  <name>Agent D: Schedule week grid — /schedule</name>
  <read_first>
    - docs/guardrails/frontend/16-schedule-view.md (week grid layout, event block anatomy, density toggle, filters)
    - docs/images/ui/schedule.png (visual reference)
    - apps/web/src/lib/fixtures/scheduleEvents.ts (FIXTURE_SCHEDULE_EVENTS — 18 events across 3 workers)
  </read_first>
  <files>
    apps/web/src/app/(app)/schedule/page.tsx,
    apps/web/src/app/(app)/schedule/_components/WeekGrid.tsx,
    apps/web/src/app/(app)/schedule/_components/EventBlock.tsx,
    apps/web/src/app/(app)/schedule/_components/WeekNav.tsx,
    apps/web/src/app/(app)/schedule/_components/ScheduleFilters.tsx,
    apps/web/src/app/(app)/schedule/_hooks/use-schedule.ts
  </files>
  <action>
    Build the schedule week grid per docs/images/ui/schedule.png and 16-schedule-view.md.

    PAGE LAYOUT:
    Top bar: page title "Schedule" (text-h1), WeekNav, ScheduleFilters, density toggle.
    Main content: WeekGrid — full-width horizontally scrollable grid.

    WEEK NAVIGATION (WeekNav):
    Prev/Next buttons (chevron icons) + "Today" button + current week label (e.g. "Jun 2–8, 2026").
    State: current week start date (Monday). Stored in useState.
    Clicking prev/next shifts by 7 days. "Today" resets to current week.
    Per CONTEXT.md Claude's Discretion: density toggle persists to localStorage.

    SCHEDULE FILTERS (ScheduleFilters):
    Worker filter (multi-select combobox using shadcn Select or a simple multi-select).
    Property filter (single select). Both filter which events show in the grid.

    WEEK GRID (WeekGrid):
    Grid layout: leftmost column = worker names (sticky left), remaining 7 columns = Mon-Sun dates.
    Header row: empty cell + 7 date headers (day name + date, e.g. "Mon 2").
    Worker rows: worker name cell (sticky left) + 7 day cells.
    Each day cell shows EventBlock components for events matching that worker + date.
    If a day has more than 3 events for a worker, show first 2 + "+N more" chip.
    Grid uses CSS grid with fixed column widths (worker col: 140px, day cols: 120px each).
    The overall grid scrolls horizontally when content overflows.

    EVENT BLOCK (EventBlock):
    Per 16-schedule-view.md: colored block with property name + time range.
    Status color: scheduled → bg-info-fade border-info, in_progress → bg-accent-fade border-accent,
    completed → bg-success-fade border-success, overdue → bg-danger-fade border-danger.
    High priority: show amber dot on left edge.
    On click: show a popover/tooltip with full event details.
    Size: rounded-md text-small px-space-2 py-space-1, left border-l-2 in status color.

    DENSITY TOGGLE:
    Two options: "Compact" (smaller event blocks, tighter row height) | "Comfortable" (default).
    Persists to localStorage key 'schedule-density'.

    use-schedule.ts: returns FIXTURE_SCHEDULE_EVENTS filtered by worker and property filter state.
    Also computes the event map: Record<workerId, Record<date, ScheduleEvent[]>>.

    No 'any'. No console.log. Typecheck-clean.
  </action>
  <verify>
    <automated>
      grep "WeekGrid" apps/web/src/app/(app)/schedule/page.tsx
      grep "EventBlock" apps/web/src/app/(app)/schedule/_components/WeekGrid.tsx
      grep "WeekNav" apps/web/src/app/(app)/schedule/page.tsx
      grep "localStorage" apps/web/src/app/(app)/schedule/_hooks/use-schedule.ts || grep "localStorage" apps/web/src/app/(app)/schedule/page.tsx
      pnpm --filter @crewmate/web typecheck
    </automated>
  </verify>
  <done>
    Schedule renders workers-in-rows × days-in-columns grid from FIXTURE_SCHEDULE_EVENTS;
    week navigation works; worker/property filters work; density toggle persists to localStorage;
    event blocks show status colors; typecheck exits 0.
  </done>
</task>

<task type="auto" id="22-AgentE">
  <name>Agent E: Worker today view — /today</name>
  <read_first>
    - docs/guardrails/frontend/15-worker-mobile.md (mobile shell spec, date strip, job card anatomy, 44px tap targets)
    - docs/images/ui/worker-mobile.png (visual reference)
    - apps/web/src/lib/fixtures/jobs.ts (FIXTURE_JOBS — filter for today's jobs for a worker)
    - apps/web/src/lib/fixtures/users.ts (FIXTURE_SESSIONS — get worker session)
  </read_first>
  <files>
    apps/web/src/app/today/page.tsx,
    apps/web/src/app/today/_components/TodayHeader.tsx,
    apps/web/src/app/today/_components/JobCardMobile.tsx,
    apps/web/src/app/today/_components/DateStrip.tsx,
    apps/web/src/app/today/_hooks/use-today.ts
  </files>
  <action>
    Build the worker today view per docs/images/ui/worker-mobile.png and 15-worker-mobile.md.
    This screen is inside the today/layout.tsx (no sidebar — mobile shell only).

    PAGE LAYOUT:
    Fixed header (TodayHeader) — 56px tall, bg-surface, border-b border-line.
      Shows "Today" title + worker name from session cookie.
    DateStrip — horizontal 7-day strip showing Mon-Sun with today highlighted.
    Vertical scrollable job list — JobCardMobile components.
    Sticky footer — sign out link.

    HEADER (TodayHeader):
    Shows "Good morning, [name]" or time-appropriate greeting + date ("Wednesday, Jun 4").
    Uses current date from JavaScript Date object.

    DATE STRIP (DateStrip):
    7 day chips in a row: Mon, Tue, Wed, Thu, Fri, Sat, Sun.
    Each shows: day abbreviation + date number (e.g. "Mon 2").
    Today's chip: bg-brand text-on-brand rounded-full.
    Other chips: text-muted bg-transparent.
    Clicking a day chip is no-op in Phase 2 (future: filter jobs by day).
    Horizontal scroll if needed (overflow-x auto). No scrollbar visible (hide with CSS).
    Min tap target 44×44px on each chip.

    JOB CARD MOBILE (JobCardMobile):
    Per 15-worker-mobile.md: full-width card showing job status.
    Status bar on left edge (4px wide, status color).
    Content: property name (text-body-strong) + address (text-small text-muted) + time chip.
    Status badge (StatusPill) in top right.
    Primary action button at bottom (full width, 44px min height):
      - "Start job" for scheduled → en_route transition (mock)
      - "Mark in progress" for en_route → in_progress (mock)
      - "Complete job" for in_progress → completed (mock)
      - No button for completed or cancelled jobs
    Button click: show a toast "Job updated (preview mode — no real API)".

    JOBS DATA: use-today.ts hook
    In Phase 2, import FIXTURE_JOBS directly and filter to jobs where status !== 'completed' && status !== 'cancelled'.
    Show all active jobs (Phase 2 doesn't filter by worker — all fixture jobs shown for demo).
    Sort by scheduledStart ascending.

    ACCESSIBILITY: Every interactive element has aria-label. Min tap target 44×44px everywhere.
    Primary action buttons are real <button> elements with the tap target enforced via padding.

    No 'any'. No console.log.
  </action>
  <verify>
    <automated>
      grep "DateStrip" apps/web/src/app/today/page.tsx
      grep "JobCardMobile" apps/web/src/app/today/page.tsx
      grep "44\|min-h-\[44" apps/web/src/app/today/_components/JobCardMobile.tsx
      grep "FIXTURE_JOBS" apps/web/src/app/today/_hooks/use-today.ts
      pnpm --filter @crewmate/web typecheck
    </automated>
  </verify>
  <done>
    Today view renders date strip + vertical job list + 44px action buttons; uses FIXTURE_JOBS;
    mock transition shows toast; mobile layout matches worker-mobile.png reference; typecheck exits 0.
  </done>
</task>

<task type="auto" id="22-AgentF">
  <name>Agent F: Webhook deliveries log — /webhooks</name>
  <read_first>
    - docs/guardrails/frontend/17-webhooks-and-events.md (table layout, detail panel, retry action, JsonViewer)
    - docs/images/ui/webhook-log.png (visual reference)
    - apps/web/src/lib/fixtures/webhookDeliveries.ts (FIXTURE_WEBHOOK_DELIVERIES — 12 deliveries)
  </read_first>
  <files>
    apps/web/src/app/(app)/webhooks/page.tsx,
    apps/web/src/app/(app)/webhooks/_components/DeliveryTable.tsx,
    apps/web/src/app/(app)/webhooks/_components/DeliveryDetailPanel.tsx,
    apps/web/src/app/(app)/webhooks/_components/JsonViewer.tsx,
    apps/web/src/app/(app)/webhooks/_hooks/use-webhooks.ts
  </files>
  <action>
    Build the webhook deliveries log per docs/images/ui/webhook-log.png and 17-webhooks-and-events.md.

    PAGE LAYOUT: Two-column layout on lg (table left 60%, detail panel right 40%).
    Single column on mobile (table full-width, detail panel below or in drawer).
    Page title "Webhooks" (text-h1).

    DELIVERY TABLE (DeliveryTable):
    Stripe-style table with columns: Status, Event type, Endpoint, Attempt, Timestamp, Latency.
    Status column: StatusPill component (delivered/retrying/failed).
    Event type: text-mono font (text-mono class) — e.g. "job.status.changed".
    Endpoint: truncated URL (max 32 chars + ellipsis) with Tooltip showing full URL.
    Attempt: "1 of 5" format, text-small text-muted.
    Timestamp: human format (e.g. "2 min ago", "yesterday at 4:12 PM") — use date-fns or Intl.RelativeTimeFormat.
    Latency: text-mono with tabular-nums class, colored: <200ms → text-success, 200-1000ms → text-warn, >1000ms → text-danger.
    Clicking a row: sets selected delivery in useState (or URL param), shows detail panel.
    Failed deliveries have a "Retry" action button in the last column (only for failed rows).
    Retry click: shows toast "Retry queued (preview mode)".

    DELIVERY DETAIL PANEL (DeliveryDetailPanel):
    Right panel (sticky right on desktop, or Sheet on mobile).
    Shows: status pill, event type, endpoint URL, attempt count, timestamp.
    Two tabs: "Request" | "Response" — shows the JSON payload for each.
    JsonViewer component: renders JSON in a code block with syntax highlighting.
    Simple implementation: pre tag with JSON.stringify(data, null, 2), bg-canvas rounded-lg p-space-4,
    text-mono text-small, overflow-x auto.

    JSONVIEWER (JsonViewer):
    Simple JSON renderer component used by DeliveryDetailPanel.
    Props: data: unknown, label?: string.
    Renders: label (text-small text-muted), then a scrollable pre block.

    use-webhooks.ts hook:
    Phase 2 — imports FIXTURE_WEBHOOK_DELIVERIES directly.
    Returns { deliveries, selectedId, setSelectedId, selectedDelivery }.

    If date-fns is not in package.json, use Intl.RelativeTimeFormat instead (no new dep needed).

    No 'any'. No console.log.
  </action>
  <verify>
    <automated>
      grep "DeliveryTable" apps/web/src/app/(app)/webhooks/page.tsx
      grep "DeliveryDetailPanel" apps/web/src/app/(app)/webhooks/page.tsx
      grep "JsonViewer" apps/web/src/app/(app)/webhooks/_components/DeliveryDetailPanel.tsx
      grep "FIXTURE_WEBHOOK_DELIVERIES" apps/web/src/app/(app)/webhooks/_hooks/use-webhooks.ts
      pnpm --filter @crewmate/web typecheck
    </automated>
  </verify>
  <done>
    Webhook log renders 12 fixture deliveries with StatusPill, latency color coding, and retry button;
    clicking a row opens detail panel with JSON viewer; typecheck exits 0.
  </done>
</task>

<task type="auto" id="22-AgentG">
  <name>Agent G: Team members + Invite dialog + Member drawer — /settings/team</name>
  <read_first>
    - docs/guardrails/frontend/18-team-and-rbac.md (team list anatomy, invite dialog, member drawer spec)
    - docs/images/ui/team-management.png (visual reference)
    - apps/web/src/lib/fixtures/teamMembers.ts (FIXTURE_TEAM_MEMBERS — 7 members incl. 1 invited)
  </read_first>
  <files>
    apps/web/src/app/(app)/settings/team/page.tsx,
    apps/web/src/app/(app)/settings/team/_components/MemberTable.tsx,
    apps/web/src/app/(app)/settings/team/_components/InviteDialog.tsx,
    apps/web/src/app/(app)/settings/team/_components/MemberDrawer.tsx,
    apps/web/src/app/(app)/settings/team/_hooks/use-team.ts
  </files>
  <action>
    Build the team settings screen per docs/images/ui/team-management.png and 18-team-and-rbac.md.

    PAGE LAYOUT:
    Header: page title "Team" (text-h1) + "Invite member" Button (primary, top right).
    Two sections: "Members" table + "Pending invitations" card (if any invited members).

    MEMBER TABLE (MemberTable):
    Columns: Member (avatar + name + email), Role, Scope, Last active, Actions.
    Avatar: colored circle with initials (avatarColor + avatarInitials from fixture).
    Role column: RolePill component.
    Scope column: small chips showing scopeLabel.
    Last active: relative time (e.g. "3 days ago") or "Invited" for pending.
    Actions column: IconButton with MoreHorizontal icon → DropdownMenu with "View", "Change role", "Revoke".
    Clicking a row OR "View" in dropdown: opens MemberDrawer.

    INVITE DIALOG (InviteDialog):
    Triggered by "Invite member" button. Uses shadcn Dialog.
    Form fields: Email (required), Full name (optional), Role (Select: Coordinator | Worker | Admin),
    Scope (appears after role selection — for Worker: property select, for Coordinator: region select).
    react-hook-form + zod validation.
    Submit: shows success Toast "Invitation sent" + closes dialog.
    Schema:
    ```ts
    const inviteSchema = z.object({
      email: z.string().email(),
      name: z.string().optional(),
      role: z.enum(['tenant_admin', 'coordinator', 'worker']),
    });
    ```

    MEMBER DRAWER (MemberDrawer):
    Right-side Sheet (480px desktop / full-width mobile).
    Header: member avatar + name + RolePill.
    Tabs: "Grants" | "Audit" | "Notifications".
    Grants tab: role grant details (role, scope, granted by, granted at).
    Audit tab: last 3 audit rows from FIXTURE_AUDIT_ROWS where actorId matches member.
    Notifications tab: notification preferences list (mock data).
    Footer actions: "Change role" button (opens nested dialog) + "Revoke access" button (danger variant, shows ConfirmDialog).

    use-team.ts: returns FIXTURE_TEAM_MEMBERS, pendingMembers (status === 'invited'), and selectedMemberId state.

    No 'any'. No console.log.
  </action>
  <verify>
    <automated>
      grep "MemberTable" apps/web/src/app/(app)/settings/team/page.tsx
      grep "InviteDialog" apps/web/src/app/(app)/settings/team/page.tsx
      grep "MemberDrawer" apps/web/src/app/(app)/settings/team/page.tsx
      grep "zodResolver" apps/web/src/app/(app)/settings/team/_components/InviteDialog.tsx
      grep "FIXTURE_TEAM_MEMBERS" apps/web/src/app/(app)/settings/team/_hooks/use-team.ts
      pnpm --filter @crewmate/web typecheck
    </automated>
  </verify>
  <done>
    Team settings renders member table with RolePill + scope chips; invite dialog has validated form;
    member drawer opens on row click with tabs; typecheck exits 0.
  </done>
</task>

<task type="auto" id="22-AgentH">
  <name>Agent H: Custom roles — /settings/team/roles</name>
  <read_first>
    - docs/guardrails/frontend/18-team-and-rbac.md (custom roles spec — built-in read-only section + custom role cards + new role dialog)
    - docs/images/ui/team-management.png (visual reference — same guardrail image covers both team and roles pages)
    - apps/web/src/lib/fixtures/customRoles.ts (FIXTURE_CUSTOM_ROLES — 2 custom roles)
  </read_first>
  <files>
    apps/web/src/app/(app)/settings/team/roles/page.tsx,
    apps/web/src/app/(app)/settings/team/roles/_components/RoleCard.tsx,
    apps/web/src/app/(app)/settings/team/roles/_components/NewRoleDialog.tsx,
    apps/web/src/app/(app)/settings/team/roles/_hooks/use-roles.ts
  </files>
  <action>
    Build the custom roles page per 18-team-and-rbac.md.

    PAGE LAYOUT:
    Header: "Roles" (text-h1) + "New role" Button (primary, top right).
    Two sections:
    1. Built-in roles (read-only): cards or list showing the 4 built-in roles with their
       permission summaries. Each shows a lock icon and "Built-in" badge. Not editable.
       Built-in roles: super_admin (All permissions), tenant_admin (Full operator access),
       coordinator (Dispatch and schedule), worker (Own day only).
    2. Custom roles section: FIXTURE_CUSTOM_ROLES rendered as RoleCard components.

    ROLE CARD (RoleCard):
    Card per custom role: name, description?, list of permissions (action × subject pairs).
    Permissions shown as tags (small pill per permission).
    Card footer: "Edit" + "Delete" buttons (both mock — show toast in Phase 2).

    NEW ROLE DIALOG (NewRoleDialog):
    Triggered by "New role" button. Uses shadcn Dialog.
    Fields: Role name (required, text input), Description (optional, textarea).
    Permissions matrix: grid of checkboxes — rows = actions (view, create, edit, delete, transition),
    columns = subjects (Jobs, Workers, Properties, Users, Webhooks, Custom roles).
    At least one permission required (zod validation).
    Submit: shows success toast + closes dialog.

    use-roles.ts: returns FIXTURE_CUSTOM_ROLES and built-in role definitions.

    No 'any'. No console.log.
  </action>
  <verify>
    <automated>
      grep "RoleCard" apps/web/src/app/(app)/settings/team/roles/page.tsx
      grep "NewRoleDialog" apps/web/src/app/(app)/settings/team/roles/page.tsx
      grep "FIXTURE_CUSTOM_ROLES" apps/web/src/app/(app)/settings/team/roles/_hooks/use-roles.ts
      grep "Checkbox\|checkbox" apps/web/src/app/(app)/settings/team/roles/_components/NewRoleDialog.tsx
      pnpm --filter @crewmate/web typecheck
    </automated>
  </verify>
  <done>
    Roles page renders 4 built-in roles (read-only) + 2 fixture custom role cards; new role dialog
    shows permissions matrix with checkboxes; typecheck exits 0.
  </done>
</task>

<task type="auto" id="22-AgentI">
  <name>Agent I: Audit log + Profile + Account + Notifications — /settings/audit + /settings/profile + /settings/account + /settings/notifications</name>
  <read_first>
    - docs/guardrails/frontend/19-settings.md (all 4 settings screens spec)
    - apps/web/src/lib/fixtures/auditRows.ts (FIXTURE_AUDIT_ROWS — 10 audit rows)
    - apps/web/src/lib/fixtures/users.ts (FIXTURE_SESSIONS — for profile page pre-filled data)
  </read_first>
  <files>
    apps/web/src/app/(app)/settings/audit/page.tsx,
    apps/web/src/app/(app)/settings/audit/_components/AuditTable.tsx,
    apps/web/src/app/(app)/settings/audit/_components/AuditDetailDrawer.tsx,
    apps/web/src/app/(app)/settings/audit/_hooks/use-audit.ts,
    apps/web/src/app/(app)/settings/profile/page.tsx,
    apps/web/src/app/(app)/settings/account/page.tsx,
    apps/web/src/app/(app)/settings/notifications/page.tsx
  </files>
  <action>
    Build 4 settings screens per 19-settings.md.

    --- AUDIT LOG (/settings/audit) ---

    PAGE LAYOUT: "Audit log" (text-h1), filter bar, table, CSV export button.

    Filter bar: date range picker (Start date + End date inputs type="date"), actor filter (text input),
    subject type filter (Select: All / Job / Worker / Property / User), decision filter (Select: All / Allow / Deny).
    Filters applied client-side against FIXTURE_AUDIT_ROWS.

    AUDIT TABLE (AuditTable):
    Columns: When (timestamp relative), Actor (name + role), Subject (type + id), Action, Decision, Reason.
    Decision column: colored pill — Allow → bg-success-fade text-success, Deny → bg-danger-fade text-danger.
    Clicking a row opens AuditDetailDrawer.
    CSV Export button (top right): shows toast "Export queued (preview mode)".

    AUDIT DETAIL DRAWER (AuditDetailDrawer):
    Right Sheet with all row fields in key-value pairs.
    Shows: timestamp, actor, action, subject, decision, reason (if Deny), requestId (text-mono).

    use-audit.ts: returns FIXTURE_AUDIT_ROWS + filtered subset based on filter state.

    --- PROFILE SETTINGS (/settings/profile) ---

    Page title "Profile". Shows 4 cards stacked:
    1. Identity card: avatar (colored circle, 64px), name, email. Edit name form with react-hook-form.
    2. Password card: "Change password" section with current/new/confirm fields. Mock submit shows toast.
    3. 2FA card: shows "2FA not configured" with "Enable 2FA" button (disabled in Phase 2 with tooltip "Coming in v1.1").
    4. Sessions card: "Sign out from all devices" button (danger variant). Mock action shows toast.
    All cards: bg-surface border border-line rounded-xl p-space-6.

    --- ACCOUNT SETTINGS (/settings/account) ---

    Page title "Account". Shows 3 cards:
    1. Operator details card: name, slug (text-mono), timezone Select, default job duration Select.
       react-hook-form pre-filled with fixture data. Save button.
    2. Preferences card: language (single Select, English only), timezone. Save button.
    3. Danger zone card (bg-danger-fade border-danger rounded-xl p-space-6):
       "Delete operator" — destructive action. Clicking opens ConfirmDialog that requires typing
       the operator slug verbatim. If typed correctly, shows toast "Delete requested (preview mode)".
       ConfirmDialog: Dialog with text input + confirm button (danger variant, disabled until slug matches).

    --- NOTIFICATION PREFERENCES (/settings/notifications) ---

    Page title "Notifications". Shows one card: per-kind email toggle matrix.
    Table with rows: each notification kind (job.assigned, job.completed, job.status.changed,
    worker.invited, webhook.delivery.failed) with:
    - Kind label and description
    - "Email" column with Switch toggle
    All switches start in states from fixture data (mix of true/false).
    Toggle state managed with useState. Save button at bottom shows toast "Preferences saved (preview mode)".

    All forms: react-hook-form + zod where applicable. No 'any'. No console.log.
  </action>
  <verify>
    <automated>
      grep "AuditTable" apps/web/src/app/(app)/settings/audit/page.tsx
      grep "AuditDetailDrawer" apps/web/src/app/(app)/settings/audit/page.tsx
      grep "useForm\|react-hook-form" apps/web/src/app/(app)/settings/profile/page.tsx
      grep "Switch" apps/web/src/app/(app)/settings/notifications/page.tsx
      grep "ConfirmDialog\|confirm\|slug" apps/web/src/app/(app)/settings/account/page.tsx
      grep "FIXTURE_AUDIT_ROWS" apps/web/src/app/(app)/settings/audit/_hooks/use-audit.ts
      pnpm --filter @crewmate/web typecheck
    </automated>
  </verify>
  <done>
    Audit log renders filterable table with detail drawer and CSV export button; profile page shows
    4 setting cards with forms; account page has danger zone with confirm-by-slug dialog; notifications
    page shows Switch toggles per notification kind; typecheck exits 0.
  </done>
</task>

</tasks>

<verification>
After ALL 9 agents complete, run:
  pnpm --filter @crewmate/web typecheck    # must exit 0 — zero errors across all screens

Per-screen spot checks:
  grep "KanbanColumn" apps/web/src/app/(app)/dispatch/page.tsx
  grep "JobStepper" apps/web/src/app/(app)/dispatch/\[jobId\]/_components/JobDetailDrawer.tsx
  grep "WeekGrid" apps/web/src/app/(app)/schedule/page.tsx
  grep "DateStrip" apps/web/src/app/today/page.tsx
  grep "DeliveryTable" apps/web/src/app/(app)/webhooks/page.tsx
  grep "InviteDialog" apps/web/src/app/(app)/settings/team/page.tsx
  grep "NewRoleDialog" apps/web/src/app/(app)/settings/team/roles/page.tsx
  grep "AuditTable" apps/web/src/app/(app)/settings/audit/page.tsx
  grep "Switch" apps/web/src/app/(app)/settings/notifications/page.tsx
</verification>

<success_criteria>
1. pnpm --filter @crewmate/web typecheck exits 0 across all 13 screen directories
2. Every screen route resolves to non-404 at localhost:3001
3. Login screen matches two-panel split layout per login.png reference
4. Dispatch board renders 4 kanban columns with JobCard components from FIXTURE_JOBS
5. Job detail drawer at /dispatch/[jobId] shows stepper + activity timeline + primary actions
6. Schedule grid shows workers in rows, 7 day columns, event blocks from FIXTURE_SCHEDULE_EVENTS
7. Today view shows DateStrip and vertical job list with 44px tap targets on action buttons
8. Webhook log shows StatusPill, latency color coding, and pinned detail panel with JSON viewer
9. Team settings shows member table + invite dialog (with validated form) + member drawer
10. Custom roles page shows built-in roles read-only + custom role cards + permissions matrix dialog
11. Audit log is filterable with row-click drawer and CSV export button
12. Profile, Account, Notifications pages render their respective form cards
13. All screens import from @/lib/fixtures — zero real API calls in the network tab
</success_criteria>

<output>
After all 9 agents complete, create `.planning/phases/02-ui-screens/02-22-SUMMARY.md` documenting:
- Which agents completed successfully vs needed rework
- Whether recharts was added (version and decision reasoning)
- Any TypeScript errors encountered across agents and how they were resolved
- Confirmation that all 13 routes return 200 in dev
- Any components deferred to Wave 2.3 polish
- Confirmation that zero real API calls appear in network tab during dev
</output>
