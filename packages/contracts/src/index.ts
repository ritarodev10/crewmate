// Shared contracts between API and web.
// DTOs, event payloads, enums, and zod schemas live here so the
// type-level surface is identical on both sides of the wire.

export * from './enums';
export * from './roles';
