{
  "name": "@crewmate/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared DTOs, GraphQL types, event payloads, and zod schemas used by both the API and the web app.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint \"src/**/*.ts\" --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
